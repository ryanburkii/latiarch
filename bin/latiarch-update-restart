#!/bin/bash

if [ "$(uname -r | sed 's/-arch/\.arch/')" != "$(pacman -Q linux | awk '{print $2}')" ]; then
  gum confirm "Linux kernel has been updated. Reboot?" && latiarch-state clear re*-required && sudo reboot now

elif [ -f "$HOME/.local/state/latiarch/reboot-required" ]; then
  gum confirm "Updates require reboot. Ready?" && latiarch-state clear re*-required && sudo reboot now

elif [ -f "$HOME/.local/state/latiarch/relaunch-required" ]; then
  gum confirm "Updates require Hyprland relaunch. Ready?" && latiarch-state clear re*-required && uwsm stop
fi

for file in "$HOME"/.local/state/latiarch/restart-*-required; do
  if [ -f "$file" ]; then
    filename=$(basename "$file")
    service=$(echo "$filename" | sed 's/restart-\(.*\)-required/\1/')
    echo "Restarting $service"
    latiarch-state clear "$filename"
    latiarch-restart-"$service"
  fi
done
